let character: string = 'Skeleton';
let health: number | string = 95;
let isAlive: boolean = true;
let skills: string[] = ['Bash', 'Counter', 'Healing'];

skills.push('dash')

//health = 'FULL';

console.log("Name: " + character + "\nHealth: " + health + "\nAlive?: " + isAlive + "\nSkills: " + skills);

